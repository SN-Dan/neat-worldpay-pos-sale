<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="terminal_payment_page" name="Worldpay Terminal Payment Page">
        <t t-call="web.html_container">
            <div id="worldpay-terminal-root"
                 t-att-data-is-refund="'1' if is_refund else '0'"/>
            <style>
                html, body, #worldpay-terminal-root {
                    margin: 0;
                    padding: 0;
                    width: 100%;
                    max-width: 100%;
                    box-sizing: border-box;
                    font-family: Arial, sans-serif;
                    background: #fff;
                    overflow-x: hidden;
                }
                .terminal-wrap {
                    padding: 16px 18px 18px;
                    width: 100%;
                    max-width: 100%;
                    margin: 0 auto;
                    box-sizing: border-box;
                }
                .terminal-wrap h2 { font-size: 16px; margin: 0 0 12px; font-weight: 600; color: #212529; }
                .notice {
                    font-size: 13px;
                    line-height: 1.45;
                    color: #0c5460;
                    background: #d1ecf1;
                    border: 1px solid #bee5eb;
                    border-radius: 4px;
                    padding: 12px 14px;
                    margin: 0 0 18px;
                    white-space: normal;
                    word-wrap: break-word;
                    overflow-wrap: break-word;
                }
                .notice.refund {
                    color: #856404;
                    background: #fff3cd;
                    border-color: #ffeeba;
                }
                .status-box {
                    display: flex;
                    align-items: center;
                    gap: 12px;
                    padding: 14px 16px;
                    border: 1px solid #dee2e6;
                    border-radius: 4px;
                    background: #f8f9fa;
                    margin-bottom: 8px;
                }
                .spinner {
                    width: 22px;
                    height: 22px;
                    border: 3px solid #dee2e6;
                    border-top-color: #007bff;
                    border-radius: 50%;
                    animation: worldpay-spin 0.8s linear infinite;
                    flex-shrink: 0;
                }
                .spinner.hidden { display: none; }
                @keyframes worldpay-spin { to { transform: rotate(360deg); } }
                .status-text {
                    flex: 1;
                    min-width: 0;
                    font-size: 14px;
                    color: #495057;
                    line-height: 1.45;
                    margin: 0;
                    white-space: normal;
                    word-wrap: break-word;
                    overflow-wrap: break-word;
                }
                .result-wrap { padding: 28px 8px; text-align: center; display: none; }
                .result-wrap.show { display: block; }
                .result-title { font-size: 22px; font-weight: 600; margin-bottom: 8px; }
                .result-title.ok { color: #198754; }
                .result-title.err { color: #dc3545; }
                .result-message {
                    font-size: 14px;
                    color: #495057;
                    margin-bottom: 0;
                    line-height: 1.5;
                    white-space: normal;
                    word-wrap: break-word;
                    overflow-wrap: break-word;
                }
                .waiting-wrap.hidden { display: none; }
            </style>
            <div class="terminal-wrap waiting-wrap" id="worldpayWaitingWrap">
                <h2 t-if="is_refund">Refund by Worldpay Terminal</h2>
                <h2 t-else="">Pay by Worldpay Terminal</h2>
                <p t-attf-class="notice#{ ' refund' if is_refund else '' }">
                    <t t-if="is_refund">
                        Complete the refund on the Worldpay terminal. This screen updates automatically when the refund finishes.
                    </t>
                    <t t-else="">
                        Complete the payment on the Worldpay terminal. This screen updates automatically when payment finishes.
                    </t>
                </p>
                <div class="status-box">
                    <div class="spinner" id="worldpaySpinner"></div>
                    <p class="status-text" id="worldpay-terminal-status">
                        <t t-if="is_refund">Waiting for refund on the terminal...</t>
                        <t t-else="">Waiting for payment on the terminal...</t>
                    </p>
                </div>
            </div>
            <div class="terminal-wrap result-wrap" id="worldpayResultWrap">
                <div class="result-title" id="worldpayResultTitle"></div>
                <div class="result-message" id="worldpayResultMessage"></div>
            </div>
            <script type="text/javascript">
                (function () {
                    const terminalId = "<t t-esc='terminal_id'/>";
                    const transactionId = "<t t-esc='transaction_id'/>";
                    const wizardId = <t t-esc='wizard_id'/>;
                    const wizardType = "<t t-esc='wizard_type'/>";
                    const paymentMethodId = <t t-esc='payment_method_id'/>;
                    const isRefund = "<t t-esc='is_refund'/>" === "True";

                    const waitingWrap = document.getElementById('worldpayWaitingWrap');
                    const resultWrap = document.getElementById('worldpayResultWrap');
                    const statusEl = document.getElementById('worldpay-terminal-status');
                    const spinnerEl = document.getElementById('worldpaySpinner');
                    const resultTitle = document.getElementById('worldpayResultTitle');
                    const resultMessage = document.getElementById('worldpayResultMessage');

                    function sleep(ms) {
                        return new Promise(function (resolve) { setTimeout(resolve, ms); });
                    }

                    function isMobileViewport() {
                        try {
                            if (window.matchMedia &amp;&amp; window.matchMedia('(max-width: 576px)').matches) {
                                return true;
                            }
                        } catch (error) {}
                        return /Android|iPhone|iPad|iPod|Mobile/i.test(window.navigator.userAgent || '');
                    }

                    function getAndroidBridge() {
                        const contexts = [];
                        try {
                            if (window.top) {
                                contexts.push(window.top);
                            }
                        } catch (error) {}
                        try {
                            if (window.parent &amp;&amp; window.parent !== window) {
                                contexts.push(window.parent);
                            }
                        } catch (error) {}
                        contexts.push(window);
                        for (let i = 0; i &lt; contexts.length; i++) {
                            const ctx = contexts[i];
                            if (!ctx) {
                                continue;
                            }
                            const bridge = ctx.AndroidInterface;
                            if (ctx.isNeatPOSAndroidApp &amp;&amp; bridge &amp;&amp; typeof bridge.onPayment === 'function') {
                                return bridge;
                            }
                        }
                        return null;
                    }

                    async function jsonRpc(route, params) {
                        const response = await fetch(route, {
                            method: 'POST',
                            credentials: 'same-origin',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify({
                                jsonrpc: '2.0',
                                method: 'call',
                                params: params,
                                id: Date.now(),
                            }),
                        });
                        const payload = await response.json();
                        return payload.result;
                    }

                    function expandWizardContainers() {
                        try {
                            const frame = window.frameElement;
                            if (!frame || !window.parent || window.parent === window) {
                                return;
                            }
                            const parentDoc = window.parent.document;
                            frame.classList.add('worldpay-terminal-iframe');
                            let el = frame.parentElement;
                            while (el) {
                                el.style.width = '100%';
                                el.style.maxWidth = '100%';
                                el.style.boxSizing = 'border-box';
                                if (el.classList.contains('modal-body') || el.classList.contains('o_content')) {
                                    break;
                                }
                                el = el.parentElement;
                            }
                            parentDoc.querySelectorAll('.worldpay-terminal-processing, .worldpay-terminal-iframe-wrap').forEach(function (node) {
                                node.style.width = '100%';
                                node.style.maxWidth = '100%';
                            });
                        } catch (error) {}
                    }

                    function applyWizardLayout() {
                        try {
                            if (!window.parent || window.parent === window) {
                                return;
                            }
                            const parentDoc = window.parent.document;
                            const dialog = parentDoc.querySelector('.modal.show .modal-dialog')
                                || parentDoc.querySelector('.o_dialog .modal-dialog')
                                || parentDoc.querySelector('.modal-dialog');
                            if (dialog) {
                                dialog.classList.add('worldpay-terminal-dialog');
                            }
                            expandWizardContainers();
                            const frame = window.frameElement;
                            if (!frame) {
                                return;
                            }
                            if (isMobileViewport()) {
                                frame.style.minHeight = '280px';
                                frame.style.height = 'auto';
                            } else {
                                frame.style.minHeight = '320px';
                                frame.style.height = '360px';
                            }
                        } catch (error) {}
                    }

                    function showResult(success, message) {
                        waitingWrap.classList.add('hidden');
                        resultWrap.classList.add('show');
                        resultTitle.textContent = success
                            ? (isRefund ? 'Refund successful' : 'Payment successful')
                            : (isRefund ? 'Refund declined' : 'Payment declined');
                        resultTitle.className = 'result-title ' + (success ? 'ok' : 'err');
                        resultMessage.textContent = message || (
                            success
                                ? (isRefund
                                    ? 'The refund was processed successfully. You may close this window.'
                                    : 'The payment was processed successfully. You may close this window.')
                                : (isRefund
                                    ? 'The refund was declined or cancelled. Please try again.'
                                    : 'The payment was declined or cancelled. Please try again.')
                        );
                        expandWizardContainers();
                        const frame = window.frameElement;
                        if (frame) {
                            if (isMobileViewport()) {
                                frame.style.minHeight = '220px';
                                frame.style.height = 'auto';
                            } else {
                                frame.style.minHeight = '240px';
                                frame.style.height = '280px';
                            }
                        }
                        try {
                            if (window.parent &amp;&amp; window.parent !== window) {
                                window.parent.postMessage({
                                    type: 'worldpay_terminal_result',
                                    success: success,
                                    wizardId: wizardId,
                                    wizardType: wizardType,
                                }, '*');
                            }
                        } catch (error) {}
                    }

                    function wakeTerminal() {
                        const androidBridge = getAndroidBridge();
                        if (androidBridge) {
                            androidBridge.onPayment("btnCard");
                        }
                    }

                    async function pollPayment() {
                        wakeTerminal();
                        applyWizardLayout();
                        while (true) {
                            try {
                                const res = await jsonRpc('/pos_worldpay/document_payment_status', {
                                    terminal_id: terminalId,
                                    transaction_id: transactionId,
                                    payment_method_id: paymentMethodId,
                                });
                                if (res &amp;&amp; res.status === 200 &amp;&amp; res.data) {
                                    if (res.data.state === 'done') {
                                        showResult(true, res.data.message);
                                        return;
                                    }
                                    if (res.data.state === 'error') {
                                        showResult(false, res.data.message);
                                        return;
                                    }
                                    statusEl.textContent = res.data.message;
                                    spinnerEl.classList.toggle('hidden', res.data.state !== 'waiting' &amp;&amp; res.data.state !== 'processing');
                                } else {
                                    statusEl.textContent = isRefund
                                        ? 'Refund request expired or was not found.'
                                        : 'Payment request expired or was not found.';
                                    showResult(false, statusEl.textContent);
                                    return;
                                }
                            } catch (error) {
                                console.error(error);
                            }
                            await sleep(2000);
                        }
                    }

                    pollPayment();
                })();
            </script>
        </t>
    </template>
</odoo>
