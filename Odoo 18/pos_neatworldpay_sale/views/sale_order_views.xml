<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_pay_by_worldpay_terminal_sale_order" model="ir.actions.server">
        <field name="name">Pay by Worldpay Terminal</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="state">code</field>
        <field name="code">if records:
    action = records.action_open_worldpay_terminal_payment_wizard()
        </field>
    </record>

    <record id="action_refund_by_worldpay_terminal_sale_order" model="ir.actions.server">
        <field name="name">Refund by Worldpay Terminal</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="state">code</field>
        <field name="code">if records:
    action = records.action_open_worldpay_terminal_refund_wizard()
        </field>
    </record>

</odoo>
